export class Client{
    name: string;
    totalFounds: number;
    email: string;


    constructor(name: string, totalFounds: number, email: string){
        this.name = name;
        this.totalFounds = totalFounds;
        this.email = email;
    }

}